#include <stdio.h>
#include <stdlib.h>

//Prends en pramaètre un int qui définira quel type d'encodage on utilisera
void hdbnArithmetique(int data){
  //hdbn
  if(data == 1 || data == 3 || data == 3){
    
  }

  else{

  }
}
